# Portfolio / Account / Identity Integrity Verification

Scope: `chunk-1.out.md` checked against the source brief and the three approved/current design documents named in the review request. Ambiguity defaults to failure.

## Blocking findings

1. **Portfolio truth has no field-level authority contract.** The proposal says broker state and the OS ledger are “fused” (candidate lines 5, 15, 23) but never defines which source wins for quantity, order status, cash, settlement, cost basis, or corrections. This is incompatible with the approved distinction between broker custody truth and append-only OS intention truth (`trading-os-design.md` 323–326). The later claim that the “OS ledger [is] authoritative” for cost basis (candidate 195) is unsafe for pre-OS/external holdings and is not reconciled with that distinction. Required: an explicit per-field authority/conflict matrix; conflicts must remain observations plus reconciliation outcomes, not silently become canonical facts.

2. **The portfolio normalizer can double count or misstate availability.** Zerodha holdings and positions are imported together, but no deterministic account/product/instrument merge rule says when they overlap. Manual entries can also duplicate broker lines. A scalar `settlement_state` cannot represent simultaneously settled, T1, pledged, liened, and authorization-blocked quantity buckets. `quantity_available = total - pledged - unsettled - open-order-reserved` (36) does not distinguish buy from sell reservations and can subtract overlapping restrictions twice. The cash rule “lower of broker cash and internal reserved cash” (79) compares unlike quantities. These defects make the current portfolio contract incorrect, not merely incomplete.

3. **Completeness and staleness semantics are contradictory.** A snapshot can be `HIGH` when 95% is ledger-confirmed while also being `LOW` because *any* broker-only line exists (68–71); no precedence lattice resolves this. Market-value coverage ignores unpriced lines and does not score missing broker segments, cash, open orders, identity mapping, or corporate-action state independently. Stale price/FX may either block or receive a haircut (78), contradicting the Day-4 acceptance criterion’s fail-closed wording (174) and the approved correctness boundary that missing/stale inputs fail closed (`trading-os-design.md` 169–172, 228). Required: orthogonal completeness dimensions, deterministic worst-state aggregation, explicit thresholds by data class, and no Day-9 opening-risk haircut fallback.

4. **Protective-action behavior conflates a kill switch with liquidation.** “Do not block kill-switch / position-closing” and “kill path forces a fresh pull” (76, 83) do not define behavior when that pull itself fails or conflicts with the ledger. The approved design says HALT does not cancel protective orders, REDUCING and HALTED are distinct, and reconciliation mismatch fails closed (`trading-os-design.md` 232–245). A close quantity cannot safely be inferred from stale custody state. Kill-state transition, existing broker-side protection, repair, and broker-confirmed reduction need separate contracts.

5. **Identity and authority are under-modelled.** `Person` is described as a natural/legal identity while `BrokerageAccount` has one `LegalOwner` that “may be joint/entity” (91, 97); this cannot faithfully represent joint holders or entity ownership. `BrokerConnection` is owned by `(Person, Broker)` (116), yet credentials may be account/app-specific and the model supplies neither an explicit connection↔account authorization grant nor an own-account authority path. `TradingMandate / Consent` conflates read consent and order authority while V1 asserts no mandate exists (101). Thus the design cannot prove who may read, reconcile, or trade which account. Separate principal/profile, owner party/ownership relation, credential connection, account access grant, read consent, and trading mandate are required, each scoped, temporal, revocable, and auditable.

6. **Cross-account isolation is asserted but not mechanically closed.** A unique `(broker_id, account_number)` omits environment/account namespace, and duplicate rejection can leak an existing cross-tenant account unless lookup and error handling are tenant-safe. The blanket claim that every artifact has one `account_ref` (161) cannot describe a multi-account household view. No authorization receipt is bound to household-view construction or caching. Conversely, making every order depend only on one account snapshot drops the approved global versioned portfolio snapshot and owner-level aggregate exposure constraint (`trading-os-design.md` 323–326). Cross-account cash/margin netting must remain prohibited, while any owner-level exposure cap needs a separately scoped, non-custodial aggregate with preserved account partitions and CAS cuts.

7. **The deterministic hot path is circular and contradicts the ontology amendment.** The proposal computes a post-trade hypothetical from candidate notional (59) and runs `PreTradeRiskCheck` before `SizingEngine` (145–151), but research is forbidden to emit size/notional. The approved order is deterministic sizing followed by risk (`trading-os-design.md` 188–198). The snapshot also omits the sealed `ValidatedDataSnapshotId`, snapshot CAS/reservation version, and complete input-cut manifest. It uses legacy `ExposureVector` (39, 163) even though the approved ontology amendment replaces that contract with `RiskOverlaySet` and moves the seam through `DecisionFeatureSet` (`trading-world-ontology-design.md` 497–524, 785–787). Its ad-hoc `OntologySnapshot` (159) also bypasses the approved `SemanticSnapshotId` plus sealed projection-receipt contract (232–277). Required flow: sealed account/owner cuts → provisional deterministic sizing → post-trade risk/compliance → CAS reservation → revalidation → submit; semantic context remains a separately sealed, tighten-only input.

8. **Day-9 live is neither reconciled with controlling gates nor credibly bounded.** Candidate Day 9 promises limited live trading (179), while the approved OS requires a paper-first 90-day forward gate (`trading-os-design.md` 13–17, 27–43) and the approved ontology requires SHADOW → PAPER_CANARY → PAPER_ACTIVE → LIVE_CANARY before live influence (613–620, 827–830). The live-domain audit proposes direct-live Tier 0 (161–172), creating a source-level contradiction; the candidate silently chooses one side instead of recording a required decision/amendment. It also compresses two broker imports, canonicalization, valuation, reconciliation, identity/permission foundations, household views, end-to-end dry run, and live activation into nine days, while external account, static-IP, entitlement, legal, and infrastructure readiness are explicit preconditions (`live-domain-opportunity-audit.md` 187–192). Day 9 can be a conditional paper/read-only vertical slice; real orders require an explicit superseding approval and all blocker evidence.

## False self-audit claims

- The terminal `verdict=PASS` is false: the truth-precedence, normalization, authority, freshness, hot-path, and activation contracts above are unresolved.
- “Size within 8–15 KB” is objectively false: the file is 25,113 bytes.
- “No false completeness claims” is false because the overlapping `HIGH`/`LOW` rules can label an incomplete snapshot inconsistently, and “everything that exists in this account” is not supportable after a partial/failed broker observation.
- “HARD RULES respected” is false because the design reintroduces the superseded `ExposureVector`, bypasses the sealed semantic-snapshot contract, and silently adopts Day-9 live despite approved paper/canary gates.
- “Decision-flow shows ... retrospective” overstates the diagram: it contains no retrospective stage, only a prose reference to later paired reads.

## Required disposition

Revise before plan integration. At minimum, freeze executable schemas for source observations versus canonical projections, quantity/cash buckets, deterministic completeness aggregation, actor/account authorization, tenant-safe account identity, portfolio-cut/CAS semantics, protective-action states, and the corrected sizing→risk sequence. Record a controlling decision resolving Day-9 live versus the approved paper/canary gates.

VERDICT: FAIL
